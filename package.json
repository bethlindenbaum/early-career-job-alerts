{
  "name": "first-look-jobs",
  "version": "1.0.0",
  "private": true,
  "description": "Early-career job monitor with SMS alerts, email digests, and application tracking.",
  "scripts": {
    "build": "node scripts/build-preferences.js && node scripts/build-config.js",
    "sync:targets": "node scripts/sync-targets.js",
    "sync:clear": "node scripts/clear-target-changes.js",
    "scan:github": "node scripts/github-scan.js",
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": { "node": ">=20" }
}
